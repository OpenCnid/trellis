# Repo Ingest Fixture

A small multi-language repository used by the Session 8 live drill
(`npm run test:repo-ingest`). The drill copies this directory into a
temporary git repository, so nothing here is ever ingested in place.
